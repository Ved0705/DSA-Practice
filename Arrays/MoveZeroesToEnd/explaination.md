Consider two pointer approach where **l** starts from zero...
Now run a loop ... under it condition: if any number is non zero: array[l]=that number and l++....
After that all the remaining ones are **0**.
